name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore Pico.sln

      - name: Build
        run: dotnet build Pico.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test Pico.sln --configuration Release --no-build
